import {FilterAsyncPipe} from './filter-async.pipe';

describe('FilterAsyncPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterAsyncPipe();
        expect(pipe).toBeTruthy();
    });
});
